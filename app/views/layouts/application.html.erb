<!DOCTYPE html>
<html class="h-full">
  <head>
    <title>BookExplorer</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full">
    <% flash.each do |type, msg| %>
      <div class="flex items-center bg-blue-500 text-white text-sm font-bold px-4 py-3" id="#flashMessage" role="alert">
        <%= image_tag "info.svg", class: 'fill-current w-4 h-4 mr-2' %>
        <p><%= msg.html_safe %></p>
      </div>
    <% end %>
    <% if user_signed_in? %>
      <%= render partial: 'layouts/nav_bar', locals: { action: action_name } %>
    <% end %>
    <div class="px-12 py-8">
      <%= yield %>
    </div>
  </body>
</html>
